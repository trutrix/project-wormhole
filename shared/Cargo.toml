[package]
name = "project-wormhole-shared"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = { version = "0.30.9", features = ["bytemuck", "zerocopy"] }
nom-derive = "0.10.1"
serde = { version = "1.0.228", features = ["derive"] }
